# Sourcing-Decision-Engine
# Sourcing Decision Engine  This repo demonstrates a sourcing decision engine that: 1) builds a feasible candidate set 2) scores suppliers using configurable weights (transparent, explainable) 3) optionally runs award optimization (min cost with constraints) 4) produces audited outputs (CSV/JSON)
